{
  "name": "myretail",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "npm install --prefix ./server && npm install --prefix ./client && npm build --prefix ./client && node ./server/bin/www"
  }
}
