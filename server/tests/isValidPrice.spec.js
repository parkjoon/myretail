describe('isValidPrice(price) helper', () => {
  it('should test that true === true', () => {
    expect(true).toBe(true);
  });

  it('returns true for any valid whole number', () => {
    expect(true).toBe(true);
  });

  it('returns true for up to 2 decimal places', () => {
    expect(true).toBe(true);
  });
});
